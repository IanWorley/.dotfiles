
- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/config]
- link: 
     ~/.config/nvim:
     path: nvim
     force: true

- shell:
	- [git submodule update --init --recursive, Installing submodules ]
